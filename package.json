{
	"name": "pure-v1",
	"version": "1.0.0",
	"devDependencies": {
		"eslint": "^4",
		"apickli": "^2",
		"cucumber": "^3",
		"prettyjson": "^1",
		"mocha": "^2",
		"eslint-config-google": "^0"
	},
	"dependencies": {
		"apigeetool": "^0.14.2"
	},
  "scripts": {
    "deploy": "./node_modules/.bin/apigeetool deployproxy -u `lpass show -u apigee.com` -p `lpass show -p apigee.com` -o ozanseymen-eval -e test -n pure-v1"
  }
}
